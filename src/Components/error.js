
import '../Static/error.css'
const Error = () => {
  window.onload(window.location = '/')
  return (
    <h1 className="error_handler">
       Redirecting to HOME if not then use the navbar to redirect
    </h1>
  );
};

export default Error;
